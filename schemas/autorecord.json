{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "enabled": {
      "type": "boolean",
      "default": true
    },
    "ignoreNextStop": {
      "type": "boolean",
      "default": false,
      "description": "When toggled on the next transition to 'intermission' (or any of the stop scenes) will be ignored. You will need to switch to a record scene and back to the stop scene for the application to stop recording."
    }
  },
  "required": [
    "enabled",
    "ignoreNextStop"
  ]
}
